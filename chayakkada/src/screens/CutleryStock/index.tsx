import {ScrollView, Text, View} from 'react-native';
import ScreenHeader from '../../components/Common/ScreenHeader';

const CutleryStock = () => {
  return (
    <View>
      <ScreenHeader title="Cutlery Stock 🍴" />
      <ScrollView>
        
      </ScrollView>
    </View>
  )
};

export default CutleryStock;
